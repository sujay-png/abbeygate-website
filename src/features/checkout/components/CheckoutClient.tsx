'use client';

import Image from 'next/image';
import Link from 'next/link';
import { ChevronDown, CreditCard, Landmark, LockKeyhole, PackageCheck, ShoppingBag, UserRound } from 'lucide-react';
import { useCart } from '@/features/cart/context/CartContext';

const formatPrice = (value: number) => new Intl.NumberFormat('en-GB', { style: 'currency', currency: 'GBP' }).format(value);
const inputClass = 'h-12 w-full border border-[var(--brand-border)] bg-white px-4 text-sm outline-none transition focus:border-brand-primary focus:ring-1 focus:ring-brand-primary';

function CheckoutHeader() {
  const { itemCount, openCart } = useCart();
  return <header className="border-b border-[var(--brand-border)] bg-white"><div className="mx-auto flex h-20 max-w-[1440px] items-center justify-between px-5 sm:px-8 lg:px-12"><Link href="/" className="flex items-center" aria-label="Abbeygate England home"><Image src="/images/logo/abbeygate-logo.png" alt="Abbeygate England" width={200} height={48} priority className="h-11 w-auto object-contain" /></Link><div className="flex items-center gap-6 text-brand-primary-dark"><Link href="/account" aria-label="Account" className="hover:text-brand-primary"><UserRound className="h-5 w-5" strokeWidth={1.8} /></Link><button type="button" onClick={openCart} aria-label="Open shopping bag" className="relative hover:text-brand-primary"><ShoppingBag className="h-5 w-5" strokeWidth={1.8} />{itemCount > 0 && <span className="absolute -right-2.5 -top-2.5 flex h-4 w-4 items-center justify-center rounded-full bg-brand-gold text-[10px] font-bold text-brand-primary-dark">{itemCount}</span>}</button></div></div></header>;
}

function CheckoutFooter() {
  return <footer className="border-t border-[var(--brand-border)] bg-white"><div className="mx-auto flex max-w-[1440px] flex-col items-center justify-between gap-4 px-5 py-7 text-center text-[13px] text-brand-grey sm:flex-row sm:px-8 sm:text-left lg:px-12"><p>Abbeygate England © 2026</p><div className="flex flex-wrap justify-center gap-x-5 gap-y-2 text-brand-primary-dark"><Link href="/returns" className="hover:underline">Refund &amp; Returns</Link><Link href="/privacy" className="hover:underline">Privacy</Link><Link href="/terms" className="hover:underline">Terms</Link><Link href="/contact" className="hover:underline">Contact</Link></div></div></footer>;
}

export function CheckoutClient() {
  const { pricedItems, itemCount, subtotal, shippingCost, shippingLabel, vatCost, total } = useCart();
  if (!pricedItems.length) return <div className="min-h-screen bg-brand-cream"><CheckoutHeader /><section className="mx-auto max-w-[760px] px-5 py-24 text-center sm:px-8"><ShoppingBag className="mx-auto mb-5 h-8 w-8 text-brand-primary" /><h1 className="text-2xl">Your bag is empty</h1><p className="mt-3 text-brand-grey">Add a product before proceeding to checkout.</p><Link href="/notebooks" className="mt-8 inline-flex bg-brand-primary px-7 py-3 text-sm font-semibold text-white hover:bg-brand-primary-dark">Explore notebooks</Link></section><CheckoutFooter /></div>;

  return <div className="min-h-screen bg-brand-cream text-brand-body"><CheckoutHeader /><section className="border-b border-[var(--brand-border)] bg-brand-tint/55"><div className="mx-auto flex max-w-[1440px] items-center justify-between gap-4 px-5 py-7 sm:px-8 lg:px-12"><h1 className="text-3xl sm:text-4xl">Checkout</h1><span className="hidden items-center gap-2 rounded-full bg-white px-4 py-2 text-[11px] font-bold uppercase tracking-[0.13em] text-brand-accent sm:flex"><LockKeyhole className="h-3.5 w-3.5" /> Secure checkout</span></div></section><div className="mx-auto grid max-w-[1440px] grid-cols-1 lg:grid-cols-[minmax(0,1fr)_minmax(380px,0.78fr)]"><form className="bg-white px-5 py-10 sm:px-8 lg:px-12 lg:py-14" onSubmit={(event) => event.preventDefault()}><div className="mx-auto max-w-[640px] space-y-10"><section><div className="mb-5 flex items-baseline justify-between gap-4"><h2 className="text-xl">Contact</h2><Link href="/account" className="text-sm font-semibold text-brand-primary hover:underline">Sign in</Link></div><label className="block text-sm font-medium" htmlFor="email">Email address</label><input id="email" name="email" type="email" autoComplete="email" required placeholder="you@company.com" className={`mt-2 ${inputClass}`} /><label className="mt-4 flex items-center gap-2 text-sm text-brand-grey"><input type="checkbox" className="h-4 w-4 accent-brand-primary" /> Keep me updated with Abbeygate news and offers</label></section><section><h2 className="mb-5 text-xl">Delivery</h2><label className="block text-sm font-medium" htmlFor="country">Country / region</label><div className="relative mt-2"><select id="country" name="country" defaultValue="GB" className={`${inputClass} appearance-none pr-10`}><option value="GB">United Kingdom</option></select><ChevronDown className="pointer-events-none absolute right-4 top-1/2 h-4 w-4 -translate-y-1/2 text-brand-grey" /></div><div className="mt-4 grid gap-4 sm:grid-cols-2"><input name="firstName" required autoComplete="given-name" placeholder="First name" className={inputClass} /><input name="lastName" required autoComplete="family-name" placeholder="Last name" className={inputClass} /></div><input name="company" autoComplete="organization" placeholder="Company (optional)" className={`mt-4 ${inputClass}`} /><input name="address1" required autoComplete="address-line1" placeholder="Address" className={`mt-4 ${inputClass}`} /><input name="address2" autoComplete="address-line2" placeholder="Apartment, suite, etc. (optional)" className={`mt-4 ${inputClass}`} /><div className="mt-4 grid gap-4 sm:grid-cols-2"><input name="city" required autoComplete="address-level2" placeholder="Town / City" className={inputClass} /><input name="postcode" required autoComplete="postal-code" placeholder="Postcode" className={inputClass} /></div><input name="phone" required autoComplete="tel" placeholder="Phone number" className={`mt-4 ${inputClass}`} /></section><section><h2 className="mb-2 text-xl">Delivery method</h2><div className="flex items-start gap-3 border border-brand-soft bg-brand-tint/60 p-4 text-sm"><PackageCheck className="mt-0.5 h-5 w-5 shrink-0 text-brand-accent" /><p><span className="font-semibold">{shippingLabel}</span><br /><span className="text-brand-grey">Your delivery option will be confirmed when the secure order service is connected.</span></p></div></section><section><h2 className="mb-2 text-xl">Payment</h2><p className="mb-4 text-sm text-brand-grey">All transactions will be secure and encrypted.</p><div className="overflow-hidden border border-[var(--brand-border)]"><label className="flex items-center justify-between gap-4 border-b border-[var(--brand-border)] bg-brand-tint/60 px-4 py-4 text-sm font-semibold"><span className="flex items-center gap-3"><input defaultChecked name="payment" type="radio" value="card" className="accent-brand-primary" /> Credit or debit card</span><CreditCard className="h-5 w-5 text-brand-primary" /></label><div className="bg-white px-4 py-4 text-sm text-brand-grey">Stripe&apos;s secure payment fields will appear here once the Stripe publishable key is configured.</div><label className="flex items-center justify-between gap-4 border-t border-[var(--brand-border)] px-4 py-4 text-sm font-semibold"><span className="flex items-center gap-3"><input name="payment" type="radio" value="bacs" className="accent-brand-primary" /> Direct bank transfer</span><Landmark className="h-5 w-5 text-brand-primary" /></label></div></section><section><label className="flex items-start gap-3 text-sm leading-6"><input required type="checkbox" className="mt-1 h-4 w-4 accent-brand-primary" /> I agree to the <Link href="/terms" className="underline">Terms &amp; Conditions</Link> and <Link href="/returns" className="underline">Refund &amp; Returns policy</Link>.</label><button type="submit" className="mt-6 w-full bg-brand-primary px-6 py-4 text-sm font-bold tracking-wide text-white hover:bg-brand-primary-dark">Secure payment setup in progress</button><p className="mt-3 text-center text-xs text-brand-grey">Order placement will be enabled after Stripe and webhook keys are added.</p></section></div></form><aside className="border-t border-[var(--brand-border)] bg-brand-cream px-5 py-10 sm:px-8 lg:sticky lg:top-0 lg:h-screen lg:overflow-y-auto lg:border-l lg:border-t-0 lg:px-10 lg:py-14"><div className="mx-auto max-w-[500px]"><div className="mb-7 flex items-baseline justify-between"><h2 className="text-xl">Order summary</h2><span className="text-sm text-brand-grey">{itemCount} item{itemCount === 1 ? '' : 's'}</span></div><div className="space-y-5 border-b border-[var(--brand-border)] pb-6">{pricedItems.map((item) => <div key={item.key} className="flex gap-4"><div className="relative h-20 w-16 shrink-0 overflow-hidden bg-white"><Image src={item.customization?.fullPreviewUrl || item.image || '/images/logo/abbeygate-logo.png'} alt="" fill sizes="64px" className="object-contain mix-blend-multiply" /></div><div className="min-w-0 flex-1"><p className="font-semibold leading-5 text-brand-primary-dark">{item.name}</p><p className="mt-1 text-xs text-brand-grey">Qty {item.quantity}{item.customization?.enabled ? ' · Customised' : ''}</p></div><strong className="shrink-0 text-sm">{formatPrice(item.lineTotal)}</strong></div>)}</div><div className="mt-6 flex gap-2"><input placeholder="Discount code" aria-label="Discount code" className="min-w-0 flex-1 border border-[var(--brand-border)] bg-white px-3 text-sm outline-none focus:border-brand-primary" /><button type="button" className="border border-brand-accent px-4 py-3 text-sm font-semibold text-brand-accent hover:bg-brand-tint">Apply</button></div><dl className="mt-7 space-y-3 text-sm"><div className="flex justify-between gap-5"><dt>Subtotal</dt><dd className="font-semibold">{formatPrice(subtotal)}</dd></div><div className="flex justify-between gap-5"><dt>{shippingLabel}</dt><dd className="font-semibold">{formatPrice(shippingCost)}</dd></div><div className="flex justify-between gap-5 text-brand-grey"><dt>VAT (20%)</dt><dd>{formatPrice(vatCost)}</dd></div><div className="mt-5 flex justify-between gap-5 border-t border-[var(--brand-border)] pt-5 text-xl font-bold text-brand-primary-dark"><dt>Total</dt><dd>{formatPrice(total)}</dd></div></dl><div className="mt-7 flex items-center gap-3 border-t border-[var(--brand-border)] pt-6 text-xs text-brand-grey"><LockKeyhole className="h-4 w-4 shrink-0 text-brand-accent" /> Your payment details are encrypted and processed securely.</div></div></aside></div><CheckoutFooter /></div>;
}
